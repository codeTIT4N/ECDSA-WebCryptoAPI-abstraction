import { assert } from "chai";
import { describe, it } from "mocha";

describe("Tests", () => {
  it("tests something", () => {
    console.log("running tests");
    assert.equal(true, true);
  });
});
